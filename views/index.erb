<table>
  <tr>
    <th>id</th>
    <th>Name</th>
    <th>Topping</th>
    <th>Quantity</th>
  </tr>
  <% for order in @orders %>
  <tr>
    <td><a href="pizza-orders/<%= order.id() %>"><%= order.id() %></a></td>
    <td><%= order.pretty_name() %></td>
    <td><%= order.topping() %></td>
    <td><%= order.quantity() %></td>
  </tr>
  <% end %>
</table>
