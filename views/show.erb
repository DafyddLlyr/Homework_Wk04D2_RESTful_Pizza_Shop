<table>
  <tr>
    <th>id</th>
    <th>Name</th>
    <th>Topping</th>
    <th>Quantity</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>
  <tr>
    <td><%= @order.id() %></td>
    <td><%= @order.pretty_name() %></td>
    <td><%= @order.topping() %></td>
    <td><%= @order.quantity() %></td>
    <td>
      <form action="/pizza-orders/<%= @order.id %>/edit">
        <input type="submit" value="Edit Order">
      </form>
    </td>
    <td>
      <form action="/pizza-order/<%= @order.id %>/delete" method="post">
       <input type="submit" value="Delete Order">
      </form>
    </td>
  </tr>
</table>
